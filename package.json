{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^18.15.10",
    "lerna": "^7.3.0",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "publish": "lerna run tsc && lerna publish"
  }
}
