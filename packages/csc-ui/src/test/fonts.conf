<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>
  <!-- Visual baselines must rasterise identically in the arm64 devcontainer
       (where `sans-serif` resolves to WenQuanYi Zen Hei) and on ubuntu-latest
       (DejaVu Sans). Liberation ships with playwright's `install` when its OS
       deps are included, on both. Passed to Chromium as FONTCONFIG_FILE by
       vitest.browser.shared.ts. A double hyphen is illegal inside an XML
       comment: one here made fontconfig reject the whole file (silently for
       Chromium), so never write a CLI flag in this comment.
       Verify: FONTCONFIG_FILE=$PWD/src/test/fonts.conf fc-match sans-serif -->
  <include ignore_missing="yes">/etc/fonts/fonts.conf</include>
  <match target="pattern">
    <test qual="any" name="family"><string>sans-serif</string></test>
    <edit name="family" mode="prepend" binding="strong"><string>Liberation Sans</string></edit>
  </match>
  <match target="pattern">
    <test qual="any" name="family"><string>system-ui</string></test>
    <edit name="family" mode="prepend" binding="strong"><string>Liberation Sans</string></edit>
  </match>
  <match target="pattern">
    <test qual="any" name="family"><string>ui-sans-serif</string></test>
    <edit name="family" mode="prepend" binding="strong"><string>Liberation Sans</string></edit>
  </match>
  <match target="pattern">
    <test qual="any" name="family"><string>museo-sans</string></test>
    <edit name="family" mode="assign" binding="strong"><string>Liberation Sans</string></edit>
  </match>
  <match target="pattern">
    <test qual="any" name="family"><string>monospace</string></test>
    <edit name="family" mode="prepend" binding="strong"><string>Liberation Mono</string></edit>
  </match>
</fontconfig>
