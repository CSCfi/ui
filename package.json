{
  "name": "root",
  "private": true,
  "packageManager": "pnpm@11.3.0",
  "devDependencies": {
    "@types/node": "^18.15.10",
    "concurrently": "^10.0.3",
    "consola": "^3.4.2",
    "husky": "^8.0.3",
    "typescript": "^6.0.3"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "prepare": "sh prepare-husky.sh",
    "dev": "concurrently --names \"ui,docs\" --prefix-colors \"magenta.bold,green.bold\" \"cd packages/csc-ui && pnpm run build:watch\" \"cd packages/csc-ui-documentation && pnpm run dev\"",
    "dev:next": "concurrently --names \"ui,docs\" --prefix-colors \"magenta.bold,green.bold\" \"cd packages/csc-ui-next && pnpm build:watch\" \"cd packages/csc-ui-documentation && pnpm dev:next\"",
    "ui": "pnpm --filter csc-ui-next",
    "clean": "rm -rf node_modules && pnpm -r clean"
  }
}
