// @ts-nocheck — documentation code sample; shown as text, never compiled here
import { CProgressCircle } from '@cscfi/csc-ui-react';

export const Basic = () => {
  return (
    <div>
      <CProgressCircle aria-label="Upload progress" value={72} />
    </div>
  );
};
