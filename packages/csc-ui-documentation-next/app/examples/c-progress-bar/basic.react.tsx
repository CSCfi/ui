// @ts-nocheck — documentation code sample; shown as text, never compiled here
import { CProgressBar } from '@cscfi/csc-ui-next-react';

export const Basic = () => {
  return (
    <div>
      <CProgressBar value={72} label="Uploading files" />
    </div>
  );
};
