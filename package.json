{
  "name": "root",
  "private": true,
  "packageManager": "pnpm@11.3.0",
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.1",
    "@types/node": "^18.15.10",
    "concurrently": "^10.0.3",
    "consola": "^3.4.2",
    "husky": "^8.0.3",
    "typescript": "^6.0.3"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "prepare": "sh prepare-husky.sh",
    "dev": "concurrently --names \"ui,docs\" --prefix-colors \"magenta.bold,green.bold\" \"cd packages/csc-ui && pnpm docs:manifest && pnpm build:watch\" \"cd packages/csc-ui-documentation && pnpm dev\"",
    "ui": "pnpm --filter csc-ui",
    "clean": "rm -rf node_modules && pnpm -r clean",
    "release": "pnpm --filter @cscfi/csc-ui build && pnpm --filter @cscfi/csc-ui-react build && changeset publish",
    "changeset": "changeset"
  }
}
