// @ts-nocheck — documentation code sample; shown as text, never compiled here
// Typed via the HTMLElementTagNameMap augmentation from @cscfi/csc-ui-next.
const cSwitch = document.createElement('c-switch');
cSwitch.setAttribute('label', 'Email notifications');
cSwitch.setAttribute('required', '');

document.body.append(cSwitch);
